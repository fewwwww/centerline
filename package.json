{
  "name": "card-centering-tool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node server.mjs",
    "preview": "npm run build && node server.mjs dist",
    "start": "node server.mjs",
    "test": "npm run build && node --test"
  }
}
