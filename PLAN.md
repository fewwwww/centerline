# 卡牌居中测量器：实施计划

> 状态：Draft v0.2
> 日期：2026-08-25
> 当前基线：v0.1 已上线；v0.2 继续保持静态站点、无登录、无后端、图片纯本地处理。

## 1. 要交付的结果

做一个同时适配手机和 PC 浏览器的卡牌居中测量页：

1. 用户上传或拍摄一张卡牌照片。
2. 页面展示覆盖在图片上的 8 条可拖拽参考线：左、右、上、下各有一条外沿线和一条内沿线。
3. 用户把外沿线对准卡片边缘，把内沿线对准卡面边框或图案边缘。
4. 页面根据左右两侧、上下两侧的间距实时计算并展示 `左 / 右` 与 `上 / 下` 百分比。
5. 页面按正面 / 背面 PSA 居中阈值，实时显示由较差轴决定的居中预估等级上限。

v0.1 只解决手动测量。v0.2 在不改变本地隐私边界的前提下，补齐手机全屏编辑、剪贴板输入、裁剪、透视校正和拍摄角度提示。

## 2. 同类工具实测结论

本轮在桌面浏览器与 390 × 844 手机视口中完成了页面检查，并上传了用户提供的 Cooper Flagg 卡图。

### 值得保留的机制

- 上传后先预览，再确认进入测量。
- 测量区确实使用 8 条线：`inside / outside × left / right / top / bottom`。
- 四个方向使用不同颜色，线上的手柄与线同色。
- 结果区同时显示左右和上下两组比例，并在拖动时更新。
- 桌面端结果在图片左侧；小屏端结果移到图片上方。
- 百分比计算遵循“单侧边框宽度 ÷ 两侧边框宽度之和”。

### 不复刻的问题

- WebP 上传成功后，参考站长时间停在 `Image loading`，没有正确进入编辑态，也没有可操作的超时或重试提示。
- 处理过程中出现遮挡编辑器的营销弹层。
- 手机端测量区使用横向滚动，拖参考线与滚动画布容易争抢手势。
- 同时显示毫米值，但照片本身没有可靠的物理标尺，容易制造虚假精度。
- 页面混入大量营销内容、评级公司规则和分享功能，偏离“快速测量”主任务。

## 3. 产品取舍

### MVP 必做

- JPG / JPEG / JFIF、PNG / APNG、WebP、AVIF、GIF、BMP、HEIC / HEIF、TIFF / TIF 上传；手机端支持直接调用相机。
- 图片预览、换图、重置参考线。
- 图片 100%–600% 缩放、围绕指针缩放和放大后拖动定位。
- 8 条参考线与实时 `左 / 右`、`上 / 下` 百分比。
- 实时显示 PSA 居中预估上限，正面 / 背面可切换，并解释由哪一轴决定。
- 鼠标、触摸、触控笔统一拖拽。
- 手机与 PC 响应式布局。
- 图片只在本地处理，刷新页面后不保留。
- 格式不支持、解码失败、图片过大、计算无效时给出明确错误和下一步动作。

### 本轮刻意不做

- 自动识别卡片外沿或内边框。
- PSA 最终评级预测，以及 BGS、SGC 等其他公司的评级映射。
- 毫米换算。
- 登录、历史记录、云端存图、数据库。
- 分享链接、结果海报、社交传播。
- 多卡批量处理。

这些能力都不是完成“手动测居中”所必需；先把手动测量的准确性和手机可用性做扎实。

## 4. 技术方案

### 4.1 架构

- 形态：静态单页 Web 应用，可部署到任意静态托管。
- 建议技术栈：Vite + TypeScript + 原生 DOM/CSS；暂不引入 UI 框架。
- 图片处理：浏览器本地解码；生成工作副本后立即释放不再使用的对象 URL。
- 交互层：普通 `<img>` 上覆盖绝对定位的 HTML 参考线和手柄，不用 Canvas 承载交互。
- 输入统一：使用 Pointer Events，覆盖鼠标、触摸和触控笔。
- 测量状态：8 条线的位置均保存为图片坐标系中的 0–1 归一化值，不保存屏幕像素。
- 视图状态：缩放倍率与平移量独立于测量状态；图片和参考线共享变换，线宽与手柄尺寸通过逆缩放保持屏幕可读性。

选择 HTML 覆盖层而不是 Canvas，是因为手柄更容易获得稳定的触控热区、键盘焦点、ARIA 标签和自动化测试定位；Canvas 留给未来的结果图导出。

### 4.2 计算模型

设 8 条线在原图坐标系中的位置为：

- 水平：`outerLeft`、`innerLeft`、`innerRight`、`outerRight`
- 垂直：`outerTop`、`innerTop`、`innerBottom`、`outerBottom`

间距：

```text
leftGap   = innerLeft - outerLeft
rightGap  = outerRight - innerRight
topGap    = innerTop - outerTop
bottomGap = outerBottom - innerBottom
```

百分比：

```text
leftPct  = leftGap / (leftGap + rightGap) × 100
rightPct = 100 - leftPct

topPct    = topGap / (topGap + bottomGap) × 100
bottomPct = 100 - topPct
```

显示规则：四舍五入到整数，并由第二个值补足到 100，避免出现 `49 + 50 = 99`。照片没有物理标尺，因此不显示毫米。

### 4.3 约束

- 线的固定顺序：`outerLeft ≤ innerLeft < innerRight ≤ outerRight`。
- 线的固定顺序：`outerTop ≤ innerTop < innerBottom ≤ outerBottom`。
- 拖动时只能沿对应轴移动。
- 参考线不得离开图片可视区域，也不得穿过相邻线。
- 两侧间距之和为 0 时，该轴不输出百分比，提示“未形成可测量边框”。
- 浏览器缩放、横竖屏切换和容器尺寸变化不能改变测量结果。
- 编辑区视图缩放和平移不能改变参考线归一化坐标或测量结果。

### 4.4 PSA 居中预估模型

在现有 measurement 纯函数层增加一个确定性映射，不引入依赖或网络请求：

```text
estimatePsaCentering(horizontalResult, verticalResult, side)
  → { gradeLabel, worstAxis, worstValue, standard }
```

- 输入使用已经展示给用户的整数比例，避免隐藏小数造成阈值与可见数字不一致。
- `worstValue = max(左, 右, 上, 下)`；同时比较两轴并返回决定结果的较差轴。
- 阈值只维护一份 `PSA_CENTERING_THRESHOLDS`，UI 文案和测试引用同一真源，不在 DOM 逻辑里复制条件分支。
- 正面依次查 55、60、65、70；背面查 75、90。超过已定义范围统一返回`PSA 6 或以下`。
- 任一轴无效时返回不可用状态；切换卡面只重算预估，不改变八条线、比例、缩放或裁剪。
- 结果是“仅居中条件下的等级上限”，不把四角、边缘、表面、印刷、真伪与 PSA 人工眼感混入模型。

## 5. 响应式与交互计划

### PC

- 整页固定为 `100vw × 100dvh`，不产生页面级滚动。
- 编辑区最大宽度受视口限制，图片完整显示，不裁切。
- 左侧或上方展示结果卡，右侧展示大图；宽屏优先两栏。
- 手柄视觉尺寸约 28px，实际命中区域不小于 36px。
- 支持键盘方向键微调；`Shift + 方向键` 为较大步长。
- 支持缩放按钮、滚轮 / 触控板、双击缩放和放大后的鼠标拖图。

### 手机

- 使用 `100dvh` 排除 iOS 动态工具栏，旧版 Safari 回退 `-webkit-fill-available`，并计入安全区 inset。
- 结果卡在图片上方，始终先看到 `左 / 右` 与 `上 / 下`。
- 图片按容器宽度完整缩放，不产生横向滚动画布。
- 手柄命中区域至少 44 × 44px；同侧两个手柄错位摆放，避免重叠。
- 页面固定在单一动态视口内；拖动手柄时锁定该指针，不带动页面位移。
- 支持双指缩放与单指拖图；缩放控件始终留在图片区内，不产生页面滚动。
- 支持横竖屏切换，结果与参考线位置保持不变。

### 视觉区分

- 左、右、上、下使用四种高对比颜色。
- 八条参考线全部使用虚线；外沿与内沿通过手柄上的“外 / 内”文字和完整 ARIA 名称区分，不能只靠颜色。
- 参考线在 100% 时为 2px；随倍率增加收细至 1px，手柄与 44px 命中区不随倍率放大。
- 手柄带“外 / 内”短标签或等价图形提示。

## 6. 实施阶段

### 阶段 A：骨架与上传

- 建立静态单页工程。
- 完成上传、拍照、预览、换图和本地解码。
- 支持 EXIF 方向；将超大图片缩放为最长边不超过 4096px 的工作副本。
- 常见格式先走浏览器原生解码；HEIC 与 TIFF 在原生失败时分别转换为 JPEG 与 PNG 工作副本。
- GIF 固定为单帧；多页 TIFF 选择像素尺寸最大的一页；SVG、PDF、PSD 与相机 RAW 明确不进入普通图片入口。

### 阶段 B：测量模型

- 写纯函数计算间距与比例。
- 实现顺序约束、边界约束、无效状态。
- 为 50/50、60/40、极薄边框、零边框和缩放不变性写单元测试。
- 为缩放焦点、平移边界、双指中点和参考线屏幕线宽写纯函数测试。
- 增加 PSA 正面 / 背面阈值、边界前后 1%、方向反转、较差轴、无效结果与免责声明测试。

### 阶段 C：编辑器

- 渲染 8 条线和手柄。
- 接入鼠标、触摸、触控笔和键盘调整。
- 实时刷新结果，提供重置与换图。
- 在结果区接入正面 / 背面选择、PSA 居中预估上限、决定轴解释和固定免责声明。

### 阶段 D：响应式与错误态

- 完成手机、平板、桌面布局。
- 增加格式错误、解码失败、图片过大、处理超时、无有效边框等状态。
- 完成焦点、ARIA、对比度和触控热区检查。

### 阶段 E：可发现性与发布

- 页面 title、description、可见 H1、Open Graph 与 Twitter 文案使用一致的“卡牌居中测量器”任务口径。
- 公开 title、description、Open Graph、Twitter 和页面正文只描述 CENTERLINE 自身功能，不出现其他产品品牌或比较定位。
- 正式构建使用公开 `SITE_URL` 生成 canonical、分享图绝对 URL、WebSite + WebPage + Person JSON-LD、sitemap 与 robots。
- sitemap 只列 canonical 首页；不使用 `meta keywords`，不虚构评分、评论或站点验证码。
- GitHub Pages 项目子路径无法控制主机根 robots 的边界写入部署文档，部署后通过 Search Console 单独提交 sitemap。

### 阶段 F：验证

- 单元测试公式与约束。
- 浏览器端到端测试上传、拖拽、重置、换图和横竖屏。
- 真机检查 iPhone Safari、Android Chrome；桌面检查 Chrome、Safari、Edge。
- 用对称边框、明显偏左、明显偏上三张测试图核对结果。

## 7. 验证矩阵

| 场景 | 预期 |
|---|---|
| 左右边框相等 | 显示 50 / 50 |
| 左边框为右边框 1.5 倍 | 显示 60 / 40 |
| 上边框为 30、下边框为 70 | 显示 30 / 70 |
| 拖动外沿线 | 对应单侧间距与比例实时变化 |
| 拖动内沿线 | 对应单侧间距与比例实时变化 |
| 线试图穿过相邻线 | 停在合法边界，不发生翻转 |
| 浏览器从桌面缩到手机宽度 | 参考线和结果不变 |
| 手机拖动手柄 | 不带动页面或图片横向滚动 |
| 放大到 200% 后拖动图片 | 图片和参考线同步平移，结果不变 |
| 200% 下拖动参考线 | 参考线可独立调整，比例实时变化 |
| 100% → 600% | 手柄保持固定屏幕尺寸，虚线由 2px 收细至 1px |
| 滚轮 / 触控板 / 双指缩放 | 围绕操作点缩放，倍率限制在 100%–600% |
| WebP 图片 | 正常进入编辑器，不无限 loading |
| AVIF、GIF、BMP 图片 | 正常进入编辑器并显示 8 条参考线 |
| HEIC 图片 | 原生解码或本地转换后进入编辑器 |
| TIFF 图片 | 原生解码或本地转换后进入编辑器；多页文件使用像素尺寸最大的一页 |
| 无边框或两侧间距均为 0 | 不显示伪造百分比，提示不可测量 |
| 正面最差轴为 55 / 45、60 / 40、65 / 35、70 / 30 | 分别显示 PSA 10、9、8、7 |
| 正面最差轴为 71 / 29 | 显示 PSA 6 或以下，不继续虚构精确等级 |
| 左右 54 / 46、上下 62 / 38 | 取上下较差轴，显示 PSA 8 并说明原因 |
| 背面 75 / 25、76 / 24、90 / 10、91 / 9 | 分别显示 PSA 10、9、9、6 或以下 |
| 任一轴无效 | PSA 预估同步清空，不沿用上次结果 |

## 8. 风险与缓解

| 风险 | 缓解 |
|---|---|
| 手机高分辨率照片占用内存过大 | 本地缩放工作副本；限制最长边和最大文件大小 |
| 手柄在小屏上难点中 | 44px 隐形热区、手柄错位、Pointer Capture |
| 页面缩放导致比例漂移 | 所有坐标归一化到图片本身，不依赖布局像素 |
| HEIC / TIFF 浏览器差异 | 原生优先；失败时加载固定版本且带 SRI 的本地转换代码，图片字节不上传 |
| TIFF 展开后内存过大 | 25MB 文件上限、约 16.8M 像素降级解码上限、4096px 工作副本上限 |
| 用户把外沿与内沿弄反 | 外/内手柄标签、完整 ARIA 名称、首次引导与顺序约束 |
| 用户误以为居中预估等于最终评级 | 主标签使用“PSA 居中预估上限”，并固定注明“不代表 PSA 最终评级” |

## 9. v0.2 粗略工作量

| 范围 | 人工工程量 | Codex 协作工程量 | 额外验收 |
|---|---:|---:|---|
| P0 手机观察窗口 | 1–2 天 | 1–2 小时 | iPhone / Android 真机 |
| P1-A PSA 居中预估 | 0.5–1 天 | 0.5–1 小时 | 阈值边界与真实拖线 |
| P1-B 剪贴板输入 | 0.5–1 天 | 0.5–1 小时 | 各浏览器权限流程 |
| P1-C 校正与裁剪 | 3–5 天 | 4–6 小时 | 大图性能与 GPU 兼容 |
| P2 拍摄角度提示 | 1–2 天 | 1–2 小时 | 真实图片标定，不含采集时间 |

完整 v0.2 预计人工约 7–11 个工程日；Codex 协作约 1–1.5 个工作日。真机手势、系统剪贴板权限和阈值标定仍需要人工验收，不能只依赖自动化测试。

## 10. v0.1 已拍板事项

- 产品名与公开域名已经确定为 CENTERLINE / `center.sny.is`。
- v0.2 优先解决手机编辑、PSA 居中预估与图片几何处理，不插入最终评级预测、结果海报或账号体系。
- v0.2 的需求真源在 PRD 5.5、8、9、13 节；以下只描述实现方案与顺序。

## 11. v0.2 根因与目标结构

### 11.1 手机图片过小的根因

当前 CSS 让 `.measurement-frame` 同时承担两种职责：

1. 它由适配后的图片自然尺寸撑开。
2. 它又设置 `overflow: hidden`，成为缩放后的裁切窗口。

图片和参考线只在这个较小矩形内缩放；外层 `.image-stage` 即使还有大量黑色空间，也不能参与查看和平移。手机布局又为站点 Header、编辑标题、操作按钮和完整结果卡预留高度，并用 `100dvh - 310px` 继续限制图片高度。因此提高 600% 上限不会扩大观察窗口，只会把内容放大在原来的小框里。

### 11.2 收敛后的 DOM 分层

```text
measurement workspace
├─ compact results bar
├─ measurement viewport        固定铺满剩余动态视口，负责裁切和手势
│  └─ image canvas             由图片 contain 后的尺寸确定，负责缩放和平移
│     ├─ source image
│     └─ normalized guides     与图片共享变换
└─ editor toolbar
```

- `measurement viewport` 与图片尺寸解耦，在手机上使用 `minmax(0, 1fr)` 吃满剩余空间。
- `image canvas` 的基础尺寸通过 `min(viewportWidth / imageWidth, viewportHeight / imageHeight)` 得到。
- 平移边界根据“变换后的 canvas 尺寸”和“viewport 尺寸”共同计算，不再只传一个 frame 宽高。
- 允许受控 overscroll，使被检查的角能进入视口中央安全区；始终保留一部分图片可见，不能把图片完全甩出画面。
- 参考线继续保存 0–1 图片坐标，视口变化、缩放、裁剪动画都不改变测量结果。

### 11.3 手机沉浸布局

```text
100dvh
├─ 紧凑结果条：约 56px
├─ 图片观察视口：1fr
└─ 底部工具条：约 56px + safe-area-inset-bottom
```

进入编辑态后隐藏站点 Header、`调整参考线`大标题和外层卡片留白。完整结果改为从紧凑结果条展开；更换图片、校正图片、重置参考线和缩放归零进入底部工具条。桌面继续保留双栏，但复用同一套 viewport / canvas 几何模型。

## 12. 图片状态与本地处理管线

```text
文件 / 相机 / 剪贴板 Blob
        ↓
统一校验与解码
        ↓
source bitmap + edit recipe（唯一真源）
        ↓
WebGL 实时预览 ← 四角 / 裁剪 / 三个滑杆
        ↓ 用户点“应用”
生成 working Blob（最长边 ≤ 4096）
        ↓
现有 8 线测量编辑器
```

编辑参数只保存一份：

```text
editRecipe = {
  sourceQuad: [topLeft, topRight, bottomRight, bottomLeft],
  cropRect,
  cropAspect,
  straighten,
  verticalPerspective,
  horizontalPerspective
}
```

- 原始 Blob / bitmap 与 `editRecipe` 是权威状态；预览矩阵、输出尺寸和拍摄角度提示均按需派生，不建立第二套可写状态。
- 用户拖动时只更新 recipe 和 GPU 预览，不反复编码图片。
- 只有点“应用”才生成一次 working Blob；更换图片或离开页面时释放 bitmap、纹理、Canvas 和对象 URL。
- 从测量态重新编辑图片时先保留原 measurement state；只有应用新的工作副本才重置参考线，取消则无副作用。

## 13. 剪贴板输入方案

同时实现两条入口，覆盖 PC 与手机浏览器差异：

1. `window` 的 `paste` 事件：从 `event.clipboardData.items` 中选第一项 `image/*`，转换为 File/Blob 后交给现有 `handleFile` 管线。
2. 可见的“粘贴图片”按钮：在用户点击时调用 `navigator.clipboard.read()`，枚举第一项可读的 `image/*` 类型。

规则：

- 只处理用户主动粘贴，不轮询剪贴板。
- 不解析粘贴 HTML 中的远程 `<img>`，不根据 URL 下载图片。
- 忽略正在输入文本的表单控件中的粘贴，避免劫持正常输入。
- 编辑态收到新图片时先请求替换确认；旧对象只有在新图片成功解码后才释放。
- 异步 Clipboard API 只在 HTTPS 工作，Safari / Firefox 可能显示一次性的系统“粘贴”确认；拒绝后回退到系统粘贴或文件选择。

## 14. 裁剪与三向校正方案

### 14.1 产品映射

| 用户表达 | 产品标签 | 数学变换 |
|---|---|---|
| 前后偏转 | 校正 | 平面内旋转（roll） |
| 上下偏转 | 上下透视 | 垂直方向单应变换 |
| 左右偏转 | 左右透视 | 水平方向单应变换 |
| 原图裁剪 | 裁剪 | 输出矩形 / 四角范围 |

Apple 相册使用的也是“校正 + 垂直 + 水平”三类几何调整。若“前后偏转”实际指的不是平面内旋转，需要在开发前三向示意图确认含义，但不影响底层统一使用单应矩阵。

### 14.2 透视计算

- 用户四个角点构成源四边形，输出目标是矩形。
- 求解一个 3 × 3 单应矩阵 `H`，预览与导出都使用逆映射：对目标像素应用 `H⁻¹` 找到源图纹理坐标，再进行双线性采样。
- 旋转、垂直透视、水平透视与裁剪矩阵先组合，最后一次采样，避免多次重编码带来的模糊。
- Canvas 2D 只提供仿射矩阵，不能准确完成四点透视；主路径使用浏览器原生 WebGL 的一个纹理、一个矩形和一个小型 fragment shader。
- 输出宽高由源四边形的对边长度估计，遵守最长边 4096px 和 GPU `MAX_TEXTURE_SIZE` 两个上限。
- WebGL 不可用或 context lost 时，裁剪与旋转回退到 Canvas 2D，上下 / 左右透视置灰并说明原因。

不引入完整 OpenCV.js：本需求只用四点单应变换，引入通用计算机视觉运行时会增加数 MB 下载和更高的 iOS 内存占用。矩阵求解器与 WebGL shader 是必要且足够的最小实现。

### 14.3 辅助线

- 三分网格和中心十字固定在目标输出矩形上，图片在网格下变换。
- 四角模式额外显示连接四角的高对比虚线和 44 × 44px 触控热区。
- 操作滑杆时网格增强显示；停止操作 800ms 后降低透明度，不完全消失。
- 接近水平、垂直或 5:7 比例时提供轻量吸附反馈；吸附只改交互步长，不偷偷覆盖用户参数。

## 15. 本地“够平铺”判断的能力边界

四个已确认角点可确定性计算：

- `rotation`：上、下边平均角度相对水平线的偏差。
- `horizontalPerspective`：左右边长度比与左右边收敛角。
- `verticalPerspective`：上下边长度比与上下边收敛角。
- `quadValidity`：凸性、面积、最短边和矩阵可逆性。

建议首版只输出三级建议，不给貌似精确的“平整度 87 分”：

| 级别 | 初始阈值（待真实图标定） | 文案 |
|---|---|---|
| 可直接测 | 旋转 ≤ 1°，对边长度差 ≤ 3%，收敛角 ≤ 1° | 拍摄角度良好 |
| 建议校正 | 旋转 ≤ 5°，对边长度差 ≤ 10%，收敛角 ≤ 4° | 建议先校正，结果会更可靠 |
| 建议重拍 | 超过上述范围、四角无效或卡片不完整 | 角度偏斜明显，建议重拍 |

这是“正拍可信度”，不是“卡片物理平整度”。单张正面图缺少深度和侧面信息；透视校正又可以把任意有效四边形映射成矩形，因此校正后的矩形不能反证卡片没有翘曲。若未来要观察物理翘曲，应要求侧面图、多角度图或受控反光拍摄，作为独立功能设计。

## 16. 实施顺序

### P0：先修手机观察窗口

- 重构 viewport / canvas DOM 与平移边界。
- 手机编辑态改为紧凑结果条 + 全屏视口 + 底部工具条。
- 保留 PC 现有结构，统一底层坐标模型。
- 增加 390 × 844、360 × 800、横屏和 iOS 动态工具栏回归检查。

### P1-A：PSA 居中预估

- 实现唯一阈值表与纯函数，接入正面 / 背面规则和较差轴解释。
- 结果与比例同帧更新；无效测量、卡面切换和重置都不能残留旧值。
- 真图验收必须从 50 / 50 实际拖过 55、60、65、70 四个边界，并核对方向反转。
- UI 全程显示“仅居中条件下的等级上限”，不使用“预计最终 PSA 分数”措辞。

### P1-B：剪贴板输入

- 接入 `paste` 事件和“粘贴图片”按钮。
- 复用现有格式、体积、HEIC / TIFF 与错误处理管线。
- 覆盖无图片、权限拒绝、多项剪贴板和替换确认。

### P1-C：校正与裁剪

- 先实现 edit recipe、四角控件、自由 / 原图 / 5:7 裁剪。
- 再实现矩阵求解、WebGL 预览与一次性导出。
- 最后接入三个滑杆、网格、原图对比、撤销 / 还原和测量状态重置规则。

### P2：拍摄角度提示

- 用合成图和明确获授权的真实卖家图建立本地测试集，只保存仓库内专用测试图，不收集用户线上图片。
- 标定三级阈值，验证对旋转、梯形透视、裁切不完整和异常四边形的判断。
- UI 始终允许用户忽略建议继续手动处理。

## 17. v0.2 验证矩阵

### 17.1 当前线上版本的真图基线

2026-08-25 使用用户提供的 Cooper Flagg WebP，通过线上页面可见的“选择图片”入口，在 390 × 844 手机视口实际操作：

| 操作 | 实测结果 | 结论 |
|---|---|---|
| 上传真实 WebP | 正常进入编辑器，八条参考线和 50 / 50 结果出现 | 图片解码与基础渲染正常 |
| 查看 100% 布局 | 外层黑色 stage 为 344 × 563px，实际 measurement frame 只有 296 × 408px | 大片可用空间没有成为观察窗口 |
| 点按五次放大到 225% | 变换后图片约 666 × 917px，但仍被 296 × 408px 的 frame 裁切 | 证实问题不是最大倍率，而是 viewport / canvas 耦合 |
| 单指拖动放大图片 | 平移状态更新为约 `80px / 135px`，图片发生移动 | 手势管线工作，但受小 frame 限制 |
| 拖动左内沿参考线 | 左右结果从 50 / 50 变成 71 / 29 | 参考线计算与真实触控拖动工作 |
| 点按重置参考线 | 恢复 50 / 50，缩放归零后显示 100% | 重置行为符合预期 |

P0 改完后必须用同一张原图、同一视口、同一组操作做前后对比。只有图片观察视口真实扩大、边角能进入中央安全区且参考线结果不漂移，才算修复；截图“看起来更大”不算充分证据。

### 17.2 必跑的用户旅程

每条旅程都从刷新后的真实页面开始，通过用户可见控件完成，不允许直接调用内部 `handleFile`、修改状态变量或注入计算结果。

| 旅程 | 用户目标 | 必须实际完成 |
|---|---|---|
| 首次手机用户 | 从相册选图并得到居中结果 | 选图 → 可选校正 → 进入全屏测量 → 双指放大 → 拖图 → 调八条线 → 读结果 |
| 实时 PSA 预估 | 理解当前居中对应的等级上限 | 调线依次跨过 55 / 45、60 / 40、65 / 35、70 / 30 → 核对 10 / 9 / 8 / 7 / 6 或以下 → 切换背面 → 核对决定轴和免责声明 |
| 卖家图背景很多 | 让小卡片变得可测 | 上传 → 四角裁剪 → 5:7 / 自由比例切换 → 应用 → 确认卡片明显变大 → 测量 |
| 角度歪斜明显 | 把梯形卡片拉正 | 调四角 → 校正 / 上下 / 左右透视 → 借助网格对齐 → 查看原图对比 → 应用 → 测量 |
| PC 快速粘贴 | 不保存文件直接测量 | 从系统复制真实图片 → Command / Ctrl + V → 校正 → 滚轮 / 触控板缩放 → 测量 |
| iPhone 粘贴 | 从照片或聊天复制后打开 | 点“粘贴图片” → 处理系统确认或拒绝 → 成功进入流程；拒绝后可回退到选择图片 |
| 更换图片但后悔 | 不丢失当前测量 | 粘贴 / 选择第二张图 → 取消替换 → 原图、八条线、倍率和结果全部保留 |
| 重新校正已有测量 | 理解状态变化 | 从测量进入校正 → 取消时状态不变；再次进入并应用时明确重置参考线 |
| 错误恢复 | 出错后仍知道下一步 | 纯文字剪贴板、损坏图、超大图、无 WebGL、无剪贴板权限分别触发 → 阅读文案 → 使用替代入口成功继续 |
| 横竖屏与工具栏变化 | 操作不中断 | 调好部分参考线 → 旋转屏幕 / 展开收起 iOS 工具栏 → 图片位置、参考线和结果保持 |
| 键盘与辅助操作 | 不依赖精细鼠标 | Tab 聚焦、方向键微调、Shift 大步调整、0 / + / -、读屏名称均可完成任务 |

### 17.3 真实图片测试集

真实图片优先，数学合成图补充。开发与自测至少覆盖：

| 图片类别 | 数量下限 | 关注点 |
|---|---:|---|
| 卡片基本占满画面的清晰正拍图 | 2 | 100% 最大化显示、参考线精调 |
| 卡片只占原图小部分、背景很多 | 2 | 本次用户反馈的核心场景、裁剪前后提升 |
| 左右透视、上下透视、旋转各明显 | 各 2 | 三个控制是否直观，网格是否真的帮助对齐 |
| 轻微偏转、肉眼难判断 | 3 | 三级拍摄角度提示阈值 |
| 横图、竖图、方图、超长图 | 各 1 | contain、全屏视口和平移边界 |
| 深色背景、浅色背景、复杂背景、反光卡套 | 各 1 | 四角可见性和辅助线对比度 |
| 无边框卡、卡片被裁掉、slab / 卡砖 | 各 1 | 不可测量提示与用户继续手动覆盖 |
| JPG、PNG、WebP、HEIC、TIFF | 各 1 张真实图 | 同一入口、方向、色彩和内存表现 |
| 损坏、超限和非图片文件 | 各 1 | 明确错误与恢复 |

用户提供的 Cooper Flagg WebP 作为固定回归样本，但不直接提交进公开仓库；自测时从本地临时路径通过真实文件选择器使用。为验证透视数学，可从真实图派生已知角度的梯形、旋转和大背景版本，派生图只作为补充，不替代原始卖家图。

### 17.4 用户视角检查清单

每跑完一条旅程，不只记录“功能能不能点”，还要逐项回答：

- 用户第一眼是否知道下一步，不需要读开发术语？
- 卡片和正在调整的边角是否足够大，手指是否遮住目标？
- 图片拖动、参考线拖动和页面滚动是否发生手势争抢？
- 任意取消、返回、权限拒绝或格式失败后，用户是否能继续且不会丢失已有工作？
- 结果变化是否与用户刚才的动作方向一致，是否可能产生反向理解？
- 用户是否能理解 PSA 数字只是居中条件下的上限，而不是卡片最终评级？
- “拍摄角度”与“卡片物理平整度”的边界是否清楚，不会让用户误信？
- 图片是否始终留在本机；Network 中是否没有 Blob、坐标和编辑参数外发？
- 从打开页面到得到可信结果，是否存在无反馈等待、隐藏手势或必须猜测的状态？

任一问题答案为“否”都不能只记为视觉建议，必须进入缺陷列表并完成修复或明确写入敞口。

### 17.5 功能与技术矩阵

| 范围 | 必验场景 |
|---|---|
| 手机布局 | 100% 图片尽可能铺满新视口；225% 可把任意边角移入中央安全区；结果和工具不遮挡手柄 |
| 视图数学 | 不同图片宽高比、横竖屏切换、缩放焦点、受控 overscroll、ResizeObserver 后结果不漂移 |
| 剪贴板 | macOS / Windows 快捷键，iOS 显式粘贴，PNG 转码，纯文字，无权限，替换现有图片 |
| 单应矩阵 | identity、已知梯形、极限但有效四边形、点序错误、近奇异矩阵、正反映射误差 |
| 图像质量 | 只进行一次最终采样；透明 / 黑边自动裁切；最长边和纹理上限；EXIF 方向正确 |
| 状态 | 取消无副作用，应用后重置参考线，还原回原图，连续换图释放旧资源 |
| PSA 规则 | 正反面阈值、边界前后 1%、较差轴、方向反转、无效状态、拖动同帧更新与免责声明 |
| 隐私 | DevTools Network 中没有图片 Blob、四角坐标或编辑参数外发 |
| 浏览器 | iPhone Safari、Android Chrome、macOS Safari / Chrome、Windows Edge |

## 18. 风险、敞口与刻意的减法

| 风险 | 处理 |
|---|---|
| iOS 大图导致 GPU / 内存峰值 | 解码后先限制 4096px；查询 `MAX_TEXTURE_SIZE`；完成立即释放纹理和 bitmap |
| WebGL context lost | 明确降级为裁剪 + 旋转；保留原图和 recipe，不能丢用户状态 |
| Safari 剪贴板权限体验不一致 | 同时保留系统 paste 事件、显式按钮和文件选择三条入口 |
| 阈值误导用户 | 只输出三级拍摄建议并注明非物理平整度；真实图片标定前不发布百分评分 |
| PSA 数字被误读成最终评级 | 明确显示“居中预估上限”和决定轴；不评估其他评级维度，不模拟 PSA 人工豁免 |
| 校正后测量线坐标失效 | 应用新工作副本时确定性重置参考线；取消不修改测量状态 |

刻意不增加后端、数据库、账号、图片上传、AI 模型、OpenCV.js 或第二套图片状态。新增的矩阵求解与 WebGL 渲染边界只服务原生 Canvas 无法表达的四点透视，是完成需求的最小必要能力。
