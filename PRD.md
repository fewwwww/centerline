# PRD：卡牌居中测量器

> 状态：Draft v0.2
> 日期：2026-08-25
> 产品形态：独立响应式 Web 工具

## 0. 本轮结论

v0.1 的“上传图片 + 手动调整 8 条参考线 + 实时查看两组居中百分比”已经形成可用基线。v0.2 解决真实使用中暴露出的四件事：

1. 手机编辑区改为沉浸式全屏画布，缩放后可使用整个可用视口查看和拖动图片。
2. 支持从系统剪贴板直接粘贴图片。
3. 测量前提供非破坏性的原图裁剪、旋转校正、上下透视和左右透视。
4. 根据用户确认的卡片四角，在本地提示拍摄角度是否足够正、是否建议校正或重拍。
5. 根据左右、上下两轴中更偏的一轴，实时显示 PSA 居中条件下的预估等级上限。

所有像素与几何运算继续只在浏览器本地完成，不上传、不调用远程图片 API、不使用 AI。产品可以判断单张照片的拍摄透视，不能把它包装成对卡片本体物理翘曲或平整度的可靠结论。

## 1. 背景与问题

球星卡和收藏卡的左右、上下边框是否均匀，会影响观感和评级判断。用户可以肉眼比较，但很难稳定地把两侧差异换算成常用的 `50 / 50`、`55 / 45`、`60 / 40` 比例。

同类工具已经证明了“图片上放置内外参考线”的方法可行，但实测存在几个关键问题：

| 问题 | 用户影响 | 本产品如何处理 |
|---|---|---|
| 图片处理可能长期停在 loading | 用户不知道要等、重试还是换格式 | 处理在本地完成；超过 5 秒显示进度，失败给出原因和重试 |
| 手机编辑区横向滚动 | 拖线容易变成滚动画布 | 图片始终适配屏幕宽度，不使用横向滚动 |
| 触控手柄偏小 | 手机上难以精确选中 | 命中区域至少 44 × 44px，手柄错位摆放 |
| 毫米值缺乏可靠标尺 | 产生不真实的精度感 | MVP 只输出比例 |
| 营销内容和弹层打断任务 | 用户完成一次测量所需步骤变多 | 页面只保留上传、测量和结果 |
| 手机图片被限制在较小的图片框中 | 即使提高倍率，实际观察窗口仍小，周围画布空间无法利用 | 把“视口”和“图片画布”拆开，手机测量态使用全屏视口 |
| 卖家图包含大量背景或拍摄角度歪斜 | 卡片主体占比小，参考线无法可靠对齐 | 测量前先裁剪并做本地透视校正 |
| 图片已经复制到剪贴板 | 还要落盘、再打开文件选择器 | 支持快捷键粘贴和显式“粘贴图片”入口 |

## 2. 目标与非目标

### 2.1 目标

- 用户无需注册，打开页面即可测量。
- 从选图到出现可拖拽编辑器不超过一个主流程。
- 手机与 PC 均能稳定拖动 8 条参考线。
- 用户可把图片放大到 600%，拖动查看边角，且已对齐的参考线跟随图片保持位置。
- 手机测量态中，画布占据除紧凑结果条和底部工具条外的全部动态视口，不再保留大标题与卡片式外壳。
- 用户可通过 `Command / Ctrl + V` 或“粘贴图片”读取剪贴板中的第一张图片。
- 用户可在测量前完成自由裁剪、5:7 裁剪、旋转校正、上下透视与左右透视，并随时还原原图。
- 校正时显示固定网格、中心十字和四角控制点；应用后生成新的本地工作副本。
- 用户确认四角后，页面在本地给出“可直接测 / 建议校正 / 建议重拍”的拍摄角度提示。
- 用户拖动任意参考线时，实时看到`PSA 居中预估上限`以及决定该结果的较差轴。
- `左 / 右` 与 `上 / 下` 百分比在拖动时即时更新。
- 屏幕尺寸变化不改变测量结果。
- 图片默认不离开本机。
- 公开页面能以一致的产品名、摘要、分享图与规范 URL 被搜索引擎和社交平台理解。

### 2.2 非目标

- 不自动判断卡片或内边框位置。
- 不预测 PSA 最终评级，也不评估四角、边缘、表面、印刷、真伪或人工眼感；只根据用户手动测出的居中比例展示 PSA 居中条件下的等级上限。
- 本期不提供 BGS、SGC 或其他公司的评级映射。
- 不判断表面、四角、边缘或真伪。
- 不保存测量历史，不做账号体系。
- 不支持批量处理。
- 本期不导出结果图片或分享链接。
- 不声称能从单张正面照片判断卡片是否物理翘曲、鼓包或弯曲。
- 不自动识别卡片四角；v0.2 由用户拖动四个角点确认，任何本地提示都允许用户忽略或覆盖。

## 3. 目标用户与使用场景

### 3.1 目标用户

- 想在送评前快速检查卡牌居中的收藏者。
- 买卖卡牌时需要更客观描述居中情况的卖家和买家。
- 使用手机拍卡、使用 PC 查看高清图的两类用户。

### 3.2 典型场景

1. 用户在手机上直接拍摄一张卡牌背面，调整参考线后读取比例。
2. 用户在 PC 上上传卖家提供的高清图，判断是否明显偏左或偏上。
3. 用户换一张图重新测量，不希望前一张图被上传或保留。
4. 用户在聊天软件或网页中复制卖家图片，切回 CENTERLINE 后直接粘贴并开始处理。
5. 用户先把带有大量背景、角度歪斜的卖家图裁到卡片四角并拉正，再进行居中测量。

## 4. 核心流程

```text
空态
  → 选择图片 / 拍照 / 粘贴图片
  → 本地解码与预览
  → 校正与裁剪（可跳过）
      → 拖动四角确认卡片范围
      → 旋转 / 上下透视 / 左右透视
      → 本地显示拍摄角度提示
      → 应用并生成工作副本
  → 进入沉浸式测量编辑器
  → 按需缩放、拖动图片查看边角
  → 调整外沿 4 条线
  → 调整内沿 4 条线
  → 实时读取左右、上下比例
  → 换图或重置
```

结果实时生成，不设置额外的“提交计算”步骤。

## 5. 页面与状态

### 5.1 空态

必显内容：

- 标题：`卡牌居中测量`
- 一句话说明：`上传卡牌图片，拖动参考线测量左右和上下边框比例。`
- 主操作：`选择图片`
- 手机端次操作：`拍照`
- 支持时显示次操作：`粘贴图片`
- PC 提示：`也可以直接按 Command / Ctrl + V`
- 拍摄提示：`请尽量从正上方拍摄，并让卡片完整入镜。`
- 隐私提示：`图片仅在当前浏览器中处理。`

支持拖拽文件到上传区，但移动端不依赖拖拽作为主入口。

### 5.2 处理态

- 选图后立即显示本地预览，不出现空白等待页。
- 正常图片目标在 2 秒内进入编辑器。
- 处理超过 2 秒显示进度提示；超过 5 秒仍未完成，展示“继续等待 / 换一张图”。
- 失败时保留重新选择入口，不自动清空用户刚选的文件名。

### 5.3 编辑态

编辑态由三部分组成：

1. 结果区：`左右居中`、`上下居中`。
2. 操作区：`重置参考线`、`更换图片`。
3. 图片测量区：图片、缩放控制、8 条参考线、8 个手柄。

结果区还需显示：

- `PSA 居中预估上限`，主值为 `10 / 9 / 8 / 7 / 6 或以下 / —`。
- `正面 / 背面`选择；默认正面，选择只在当前图片会话中保留。
- 解释行，例如`按上下 62 / 38，正面标准预估为 PSA 8`。
- 固定免责声明：`仅根据居中估算等级上限，不代表 PSA 最终评级。`

首次进入显示一次简短引导：

> 先把标有“外”的虚线对准卡片外沿，再把标有“内”的虚线对准卡面边框内沿。

### 5.4 无效测量态

出现以下任一情况时，该轴不展示百分比：

- 两侧边框间距之和为 0。
- 参考线尚未形成合法顺序。
- 图片没有可识别的实体边框，例如全出血设计。

文案：`当前没有形成可测量的边框，请调整参考线。`

### 5.5 校正与裁剪态

校正与裁剪是测量前的可跳过步骤，保留原图和一份编辑参数，不直接覆盖用户文件。

- 顶部：`取消`、`还原`、`应用`。
- 中部：尽可能占满可用视口的图片预览。
- 图片上层：四角控制点、卡片边界轮廓、三分网格、中心十字。
- 拖动角点时，在手指旁显示以当前角点为中心的局部放大镜；放大镜自动避开手指和画面边缘，松手立即消失。
- 底部工具：`裁剪`、`校正`、`上下透视`、`左右透视`。
- 裁剪比例：`自由`、`原图`、`5:7 卡牌`；默认自由裁剪。
- `校正`对应平面内旋转；`上下透视`与`左右透视`对应 Apple 相册中的垂直、水平倾斜。三个滑杆都以 0 为中点，双击或点按数值恢复 0。
- 拖动任意四角时实时更新透视预览；四点必须保持凸四边形，不得交叉或退化成接近一条线。
- 应用后自动裁掉透明或黑色空区，生成新的本地工作副本，再进入测量态。
- 从测量态再次进入校正并应用时，八条测量线恢复默认值；取消则完整保留当前测量。
- 长按或按住“查看原图”时临时显示未编辑版本，松手恢复当前预览。

拍摄角度提示只描述照片几何：

| 级别 | 展示 | 行为 |
|---|---|---|
| 可直接测 | `拍摄角度良好` | 允许直接进入测量 |
| 轻微透视 | `建议先校正，结果会更可靠` | 突出校正入口，但允许跳过 |
| 严重透视或四角无效 | `角度偏斜明显，建议重拍` | 仍允许手动处理，不阻断用户 |

页面必须同时注明：`这里只判断照片拍摄角度，不代表卡片本体是否平整。`

## 6. 参考线交互规则

### 6.1 参考线闭集

| 方向 | 外沿线 | 内沿线 | 颜色角色 |
|---|---|---|---|
| 左 | 卡片左外沿 | 左侧图案/边框内沿 | 左侧色 |
| 右 | 卡片右外沿 | 右侧图案/边框内沿 | 右侧色 |
| 上 | 卡片上外沿 | 上侧图案/边框内沿 | 上侧色 |
| 下 | 卡片下外沿 | 下侧图案/边框内沿 | 下侧色 |

八条参考线全部使用虚线。外沿与内沿由手柄上的“外 / 内”文字区分；每个手柄必须有“左外沿”“左内沿”等完整 ARIA 名称。

### 6.2 拖动

- 垂直线只能左右移动；水平线只能上下移动。
- 拖动开始后锁定当前指针，直到松手或取消。
- 参考线不可移出图片。
- 参考线不可穿过同轴的相邻线。
- 拖动过程中结果每帧更新，不等待松手。
- 结果变化不触发页面跳动或重排。

### 6.3 键盘

- 手柄可获得键盘焦点。
- 方向键沿合法轴微调。
- `Shift + 方向键` 使用较大步长。
- 非法方向键不移动手柄。

### 6.4 重置

`重置参考线`恢复默认位置，不更换图片。默认位置只用于帮助用户开始，不能被描述为自动识别结果。

### 6.5 图片缩放与平移

- 倍率范围为 100%–600%；提供缩小、恢复 100%、放大三个可见按钮。
- PC 支持滚轮 / 触控板和双击缩放；手机支持双指缩放。缩放应尽量围绕指针或双指中点发生。
- 只有放大后才能拖动图片；平移被限制在图片边界内，不能把整张图拖出查看区。
- 编辑器必须把固定大小的“观察视口”和随缩放移动的“图片画布”分成两层；观察视口占满图片区域，不能继续使用图片自身尺寸充当裁切窗口。
- 100% 时图片画布以 contain 方式尽可能铺满观察视口；放大后图片和参考线在观察视口内共同变换，周围原本空闲的黑色区域也属于可观察空间。
- 图片与参考线共享同一缩放和平移变换，已对齐的位置不能因查看操作而漂移。
- 缩放和平移只改变视图，不改变参考线的 0–1 归一化坐标和测量结果。
- 手柄及其触控热区保持固定屏幕尺寸；虚线从 100% 时的 2px 随倍率增加收细，最低为 1px，不跟随图片等比例变粗。
- 键盘聚焦图片区后，`+` / `-` 缩放，`0` 恢复 100%，方向键在放大状态下平移图片。

## 7. 计算口径

### 7.1 数据来源

| 展示项 | 来源 | 刷新时机 | 不可用时 |
|---|---|---|---|
| 左侧间距 | 左外沿线到左内沿线的图片坐标距离 | 拖动每帧 | 隐藏该轴比例 |
| 右侧间距 | 右内沿线到右外沿线的图片坐标距离 | 拖动每帧 | 隐藏该轴比例 |
| 上侧间距 | 上外沿线到上内沿线的图片坐标距离 | 拖动每帧 | 隐藏该轴比例 |
| 下侧间距 | 下内沿线到下外沿线的图片坐标距离 | 拖动每帧 | 隐藏该轴比例 |
| 左右比例 | 左、右两侧间距确定性计算 | 任一左右线变化 | 显示不可测量提示 |
| 上下比例 | 上、下两侧间距确定性计算 | 任一上下线变化 | 显示不可测量提示 |

### 7.2 公式

```text
左% = 左侧间距 ÷（左侧间距 + 右侧间距）× 100
右% = 100 - 左%

上% = 上侧间距 ÷（上侧间距 + 下侧间距）× 100
下% = 100 - 上%
```

### 7.3 显示规则

- 以整数显示，例如 `49 / 51`、`60 / 40`。
- 先四舍五入第一个值，第二个值用 `100 - 第一个值`，保证总和始终为 100。
- 保留方向，不自动把较大值放在前面；左边更宽必须显示为 `60 / 40`，不能显示成无方向的 `40 / 60`。
- 不显示毫米、厘米或像素。
- 结果区注明：`仅根据居中估算等级上限，不代表 PSA 最终评级。`

### 7.4 PSA 居中预估上限

这是一条纯本地、确定性的规则映射，不调用 PSA 或其他外部接口，也不使用 AI。

先从已经显示给用户的整数比例计算每一轴的偏心值：

```text
horizontalWorst = max(左%, 右%)
verticalWorst   = max(上%, 下%)
cardWorst       = max(horizontalWorst, verticalWorst)
```

PSA 官方说明居中判断取左右与上下中更偏的一方。本产品同样以 `cardWorst` 作为唯一查表输入，并同时显示是左右还是上下决定了预估；两轴相同时显示`左右与上下`。

#### 正面规则（默认）

| `cardWorst` | 展示 |
|---:|---|
| ≤ 55 | `PSA 10` |
| 56–60 | `PSA 9` |
| 61–65 | `PSA 8` |
| 66–70 | `PSA 7` |
| > 70 | `PSA 6 或以下` |

#### 背面规则

| `cardWorst` | 展示 |
|---:|---|
| ≤ 75 | `PSA 10` |
| 76–90 | `PSA 9` |
| > 90 | `PSA 6 或以下` |

PSA 公布的背面居中阈值对 9、8、7 都是 90/10，单凭居中无法进一步区分 9、8、7。因此 76–90 只展示满足的最高居中等级 `PSA 9`，不伪造中间分数。

边界规则：

- 查表使用页面已经展示的整数比例，不使用隐藏小数，避免用户看到 `55 / 45`却得到 PSA 9。
- `55 / 45`与`45 / 55`完全等价，只看较大的一侧。
- 任一轴无效时显示`—`和`请先完成四边参考线`，不沿用上一次分数。
- PSA 官方允许评级师基于整体观感做少量个案判断；确定性工具不模拟该主观豁免。
- `PSA 6 或以下`只表示超出本 PRD 已定义的 7–10 居中阈值，不继续凭居中虚构 1–6 的精确分数。

## 8. 图片输入契约

### 8.1 支持格式

支持以下静态栅格图片输入：

- JPEG / JPG / JFIF
- PNG / APNG
- WebP
- AVIF
- GIF；进入编辑器前固定为单帧，避免动画改变测量对象
- BMP
- HEIC / HEIF；浏览器不能原生解码时，在本地转换为 JPEG 工作副本
- TIFF / TIF；浏览器不能原生解码时，在本地转换为 PNG 工作副本，多页文件选择像素尺寸最大的一页

不接收 SVG、PDF、EPS、AI、PSD / PSB 和相机 RAW。它们分别属于矢量、文档、设计源文件或传感器原始数据，不纳入普通照片测量入口。

剪贴板输入与文件输入进入同一条校验和解码管线：

- 全局监听用户发起的 `paste` 事件，读取 `clipboardData.items` 中第一项 `image/*`。
- 显式“粘贴图片”按钮在 HTTPS 环境中调用 `navigator.clipboard.read()`；浏览器可能显示系统粘贴确认。
- 剪贴板只有文字、HTML 或 URL 时不自动联网抓图，提示`剪贴板里没有可读取的图片`。
- 已在编辑图片时收到新图片，先显示`用粘贴的图片替换当前图片？`，确认前不清空当前状态。
- 浏览器可能把复制的 JPG 转成 PNG；产品按实际 Blob 类型处理，不依赖原文件名。

手机端 `拍照`入口使用后置摄像头。HEIC 与 TIFF 均优先使用浏览器原生能力；兼容转换只下载固定版本、带完整性校验的解码器代码，图片字节不会上传。首次使用兼容转换时需要网络连接，后续是否离线可用取决于浏览器缓存。

### 8.2 体积与性能

- 单文件最大 25MB。
- 解码后的工作图片最长边不超过 4096px。
- TIFF 的 JavaScript 降级解码最多接受约 16.8M 像素，避免在手机浏览器中展开超大 RGBA 缓冲区。
- 导入时对超大图片生成缩小工作副本，不改变边框比例；编辑区视图缩放不再次编码图片。
- 12MP JPEG 在目标设备上应在 2 秒内进入可操作编辑器；更大图片在 5 秒内给出可见进度或明确错误。
- 校正预览在拖动滑杆或四角时维持可感知的实时反馈；最终应用可显示短暂处理态，但不能阻塞界面且必须可取消。
- 透视处理沿用最长边 4096px 的工作副本上限，完成后立即释放旧的 GPU 纹理、Canvas 和临时 Blob。

### 8.3 隐私

- 图片只在当前浏览器内存中处理。
- 不上传服务器，不进入分析埋点，不写入 localStorage 或 IndexedDB。
- 剪贴板、裁剪、透视变换与拍摄角度提示均在浏览器本地完成；运行时不把像素、四角坐标或编辑参数发到网络。
- 换图、刷新或关闭页面后释放图片对象。
- 埋点如后续增加，只允许记录格式、体积分桶、成功/失败类型和耗时，不记录图片内容、文件名或可还原图片的信息。

## 9. 响应式规则

### 9.1 PC

- 宽屏采用结果区 + 图片区双栏布局。
- 整页固定在一个动态视口内，不出现页面级纵向或横向滚动。
- 100% 时图片与结果完整包含在可用区域内；主动放大后只在图片查看区内裁切内容。
- 结果区在拖动时保持可见。

### 9.2 手机

- 选图后进入沉浸式测量态：隐藏站点 Header、大标题和卡片式留白。
- 顺序固定为：顶部紧凑结果条 → 全屏图片观察视口 → 底部工具条。
- 顶部结果条只保留`左右 50/50`、`上下 50/50`和展开按钮；展开后再显示完整结果说明。
- 图片观察视口占据结果条、工具条与安全区之外的所有空间，宽高均为明确的布局尺寸，而不是由图片自然尺寸撑开。
- 图片保持原始宽高比，在 100% 时尽可能铺满观察视口；裁剪后的卡片主体应明显大于当前版本。
- 缩放控件完整留在图片区内；双指缩放和单指拖图不带动页面。
- 手柄真实点击区域至少 44 × 44px。
- 同侧外沿与内沿手柄错位，避免互相遮挡。
- 整页固定在一个动态视口内；拖动手柄不会带动页面位移。
- 横竖屏切换后参考线与结果保持不变。

### 9.3 断点原则

布局由可用宽度决定，不绑定具体设备型号。小屏切换为单栏，大屏切换为双栏；任何宽度下都不得裁掉图片或结果。

- 页面占用 `100vw × 100dvh`；`100vh` 仅作为不支持动态视口单位时的回退。
- 旧版 iOS Safari 使用 `-webkit-fill-available` 回退，避免底部工具栏覆盖内容。
- 顶部、底部和横向内边距计入 `env(safe-area-inset-*)`，适配刘海与 Home Indicator。
- `html`、`body` 和应用根节点禁止页面级 overflow；极矮视口只允许局部面板内部降级滚动。

## 10. 失败与降级

| 失败 | 用户看到什么 | 可采取动作 |
|---|---|---|
| 文件不是支持的栅格图片 | `请选择 JPG、PNG、WebP、HEIC、AVIF、GIF、BMP 或 TIFF 图片。` | 重新选择 |
| 文件超过 25MB | `图片过大，请选择 25MB 以内的图片。` | 重新选择或压缩 |
| 图片无法解码 | `当前浏览器无法解码该格式，或文件可能已经损坏。` | 换一张图或转为常用格式 |
| HEIC 转换失败 | `HEIC 图片无法读取。` | 下载 iCloud 原图或转为 JPG |
| TIFF 转换失败 | `TIFF 图片无法读取。` | 导出单张 PNG 后重试 |
| 处理超过 5 秒 | `图片处理时间较长。` | 继续等待或换图 |
| 两侧间距均为 0 | `当前没有形成可测量的边框。` | 调整参考线 |
| 图片过暗、模糊或未完整入镜 | `图片可能难以对齐，请换一张更清晰、完整的照片。` | 继续手动测量或换图 |
| 剪贴板无图片 | `剪贴板里没有可读取的图片。` | 重新复制图片或选择文件 |
| 剪贴板权限被拒绝 | `浏览器没有允许读取剪贴板。` | 使用系统粘贴操作或选择文件 |
| 四角交叉或面积过小 | `四个角还没有形成有效的卡片范围。` | 调整四角或还原 |
| 设备无法进行透视变换 | `当前浏览器无法进行透视校正。` | 仍可裁剪、旋转或直接测量 |

任何失败都不能只显示“失败”或一直 loading。

## 11. 搜索与分享呈现

- 搜索标题明确包含“卡牌居中测量器”，并与页面可见主标题表达同一任务。
- 公开 SEO 元数据、分享卡片和页面正文只描述 CENTERLINE 自身功能，不使用其他产品品牌做比较定位。
- 页面提供唯一且准确的 description，不添加无效的 `meta keywords` 或堆砌搜索词。
- 正式构建基于真实公开地址生成自引用 canonical、Open Graph / Twitter 分享地址与绝对图片 URL。
- 结构化数据只声明页面真实具备的 WebSite、WebPage、作者和分享图信息；没有真实评论时不声明 SoftwareApplication 富结果、评分或评论。
- sitemap 只列出希望收录的 canonical 首页，不把图片处理状态或片段 URL 当成独立页面。
- robots 默认允许抓取并引用 sitemap；GitHub Pages 项目子路径无法控制主机根 robots 时，必须在部署说明中明确，并通过 Search Console 单独提交 sitemap。
- 搜索抓取不得读取、上传或索引用户选择的本地图片。

## 12. 可访问性

- 所有操作无需依赖颜色识别。
- 手柄有可读名称、焦点样式和键盘控制。
- 文本与背景对比度达到 WCAG AA。
- 动态结果使用温和的 live region 更新，避免每个拖动像素都被读屏重复播报；松手后播报最终结果。
- 系统开启减少动态效果时，不使用非必要动画。

## 13. 验收标准

以下勾选项不能只靠单元测试、函数调用或静态截图判定通过。每条主流程必须至少使用一张真实卡片图片，从用户可见入口进入，实际完成点击、粘贴、拖动、双指 / 滚轮缩放、取消、重置和错误恢复，并保留操作前后截图、关键数值、Console 与 Network 证据。合成图只用于验证已知几何结果，不能替代真实图片体验验收。

### 上传与处理

- [ ] JPG、PNG、WebP 均能进入编辑器。
- [ ] AVIF、GIF、BMP、HEIC、TIFF 均能进入编辑器；GIF 使用固定帧，多页 TIFF 的选页规则确定。
- [ ] 用户提供的 Cooper Flagg WebP 类型图片不会无限停在 loading。
- [ ] 手机可从相册选图并直接拍照。
- [ ] 不支持格式、过大文件和解码失败均有明确文案和重试入口。
- [ ] 原生可解码格式在离线时仍能完成上传、拖动和计算；HEIC / TIFF 降级转换首次使用时明确依赖解码器代码下载。
- [ ] PC 可用 Command / Ctrl + V 粘贴 JPG、PNG 或系统提供的图片 Blob 并进入同一处理流程。
- [ ] iOS Safari 与 Android Chrome 在支持异步剪贴板时可通过“粘贴图片”读取；权限失败不影响选择图片与拍照。
- [ ] 粘贴文字或 URL 不发起远程图片请求。

### 校正与裁剪

- [ ] 自由裁剪、原图比例与 5:7 比例都能生成正确工作副本，且原始文件未被覆盖。
- [ ] 旋转、上下透视、左右透视三个滑杆可独立调整并一键归零。
- [ ] 四角拖动过程中始终显示三分网格、中心十字和有效四边形轮廓。
- [ ] 四角拖动过程中局部放大镜清晰显示当前像素，自动避开手指与画面边缘；原图对比按钮不覆盖任何角点。
- [ ] 单位矩形的透视变换保持像素位置；已知梯形可被映射为目标矩形，正反变换误差在约定容差内。
- [ ] 交叉四角、近零面积和无法求逆的矩阵不会生成损坏图片。
- [ ] 应用校正后旧测量线重置；取消校正后图片、参考线与结果完全不变。
- [ ] 整个校正流程不产生图片上传请求，也不调用 AI 服务。
- [ ] 页面只输出拍摄角度建议，不把建议描述为卡片物理平整度检测。

### 计算

- [ ] 左右等宽显示 `50 / 50`。
- [ ] 左 3、右 2 显示 `60 / 40`。
- [ ] 上 2、下 3 显示 `40 / 60`。
- [ ] 两个结果的两侧之和始终为 100。
- [ ] 结果保留方向，不做大小值自动排序。
- [ ] 屏幕缩放和横竖屏切换不改变结果。
- [ ] 正面 55 / 45、56 / 44、60 / 40、61 / 39、65 / 35、66 / 34、70 / 30、71 / 29 分别显示 PSA 10、9、9、8、8、7、7、6 或以下。
- [ ] 左右 54 / 46、上下 62 / 38 时，按较差的上下轴显示 PSA 8，并说明决定轴。
- [ ] 方向反转不改变等级：45 / 55 与 55 / 45 相同。
- [ ] 背面 75 / 25 显示 PSA 10，76 / 24 与 90 / 10 显示 PSA 9，91 / 9 显示 PSA 6 或以下。
- [ ] 任一轴无效时 PSA 预估立即清空为`—`，恢复有效后再实时计算。
- [ ] 拖动参考线过程中 PSA 预估与比例同帧更新，重置参考线后恢复对应的 50 / 50 结果。
- [ ] 页面始终显示`仅根据居中估算等级上限，不代表 PSA 最终评级`。

### 交互

- [ ] 8 条线都能通过鼠标拖动。
- [ ] 8 条线都能通过手机触摸拖动。
- [ ] 8 条线都能获得键盘焦点并微调。
- [ ] 参考线不能离开图片或穿过相邻线。
- [ ] 按钮、滚轮 / 触控板、双击和手机双指均可在 100%–600% 范围内缩放。
- [ ] 放大后可拖动图片；图片与参考线同步位移，测量结果不变。
- [ ] 放大状态仍能单独拖动参考线并实时更新结果。
- [ ] 200% 时手柄屏幕尺寸不变，虚线比 100% 更细且不低于 1px。
- [ ] 恢复 100% 会清空平移，但不会重置参考线。
- [ ] 手机拖线前后页面都固定在单一动态视口内，不发生页面位移。
- [ ] 重置参考线不更换图片；更换图片会清空旧测量状态。

### 响应式

- [ ] 360px 宽度下无横向页面滚动。
- [ ] 390px 手机竖屏、手机横屏均可完成测量。
- [ ] 768px 平板和 1280px 桌面均能完整显示结果与图片。
- [ ] iOS Safari 地址栏或底部工具栏展开、收起时，界面仍贴合 `100dvh` 且关键操作不被安全区遮挡。
- [ ] 当前及前一个主要版本的 Chrome、Safari、Edge 可用。
- [ ] 390 × 844 手机测量态隐藏站点 Header 和大标题，图片观察视口覆盖除紧凑结果条、底部工具条与安全区外的空间。
- [ ] 任意缩放倍率下，图片的四角都能通过平移进入观察视口中央附近，不再受原始图片框尺寸裁切。
- [ ] 至少使用一张“卡片占满画面”和一张“卡片只占原图小部分”的真实图片完成手机全流程，确认裁剪前后的可观察尺寸确实改善，而不是只提高 CSS 倍率。

### 隐私

- [ ] 测量流程不发起图片上传请求。
- [ ] 图片内容、文件名和测量坐标不进入埋点。
- [ ] 换图或离开页面后释放旧图片对象。

### 搜索与分享

- [ ] 正式构建中的 canonical、Open Graph URL、Twitter 图片、JSON-LD、sitemap 使用同一个真实公开地址。
- [ ] sitemap 只包含 canonical 首页，且使用 UTF-8 与绝对 URL。
- [ ] 页面提供 1:1 favicon、可读 title / description、`og:image:alt` 与明确的中文语言标记。
- [ ] 不存在占位域名、`meta keywords`、虚构评分、虚构评论或 Search Console 验证码。
- [ ] 部署后通过 Search Console URL Inspection 检查实际抓取结果，并提交 sitemap。

## 14. 后续候选

按优先级从高到低：

1. 下载带参考线与比例的结果图。
2. 可选的 BGS / SGC 居中标准对照。
3. 使用传统边缘算法辅助定位卡片四角；必须允许用户完全手动覆盖，不能包装成 AI 判断。
4. 多角度或侧面照片辅助观察卡片翘曲；单张正面图不做物理平整度承诺。

## 15. 待拍板

1. 用户所说的“前后偏转”是否就是 Apple 相册中的`校正`（平面内旋转）；本 PRD 暂按这一含义设计。
2. “可直接测 / 建议校正 / 建议重拍”的几何阈值需要用一批真实卖家图标定，首版不能宣称为行业标准。
3. 是否需要保存最近一次测量；若需要，将改变当前“刷新即清空”的隐私边界。

## 16. 调研依据与可行性结论

- Apple 的相册交互本质上是裁剪、校正、垂直倾斜与水平倾斜，本产品采用同样的任务口径，但界面针对卡片四角对齐优化：[Apple iPhone 照片裁剪与校正文档](https://support.apple.com/zh-cn/guide/iphone/iph0f3ebb1dd/ios)。
- PSA 官方公开的正面居中阈值为 10/9/8/7 对应约 55/45、60/40、65/35、70/30，背面为 PSA 10 的 75/25 与 PSA 9–7 的 90/10；官方还说明左右、上下取更偏的一轴，并保留基于整体观感的人工判断空间：[PSA 官方评级标准](https://www.psacard.com/gradingstandards)、[PSA Japan 完整等级说明](https://www.psacard.com/ja-JP/gradingstandards)。
- 浏览器的 `paste` 事件可从 `ClipboardEvent.clipboardData` 读取用户粘贴的数据；异步 `navigator.clipboard.read()` 可读取图片，但只在 HTTPS 下工作，并受用户手势、权限或系统粘贴确认约束：[MDN ClipboardEvent](https://developer.mozilla.org/en-US/docs/Web/API/ClipboardEvent/clipboardData)、[MDN Clipboard.read()](https://developer.mozilla.org/en-US/docs/Web/API/Clipboard/read)。
- Canvas 2D 原生矩阵只覆盖缩放、旋转、平移与倾斜等仿射变换，不能直接完成四点透视；四角拉正需要 3 × 3 单应矩阵和透视重采样：[MDN Canvas transform](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/transform)、[OpenCV 透视变换说明](https://docs.opencv.org/5.0/js_tutorials/js_imgproc/js_geometric_transformations/js_geometric_transformations.html)。
- v0.2 不需要引入完整 OpenCV.js。需求只涉及四点单应变换，可用一个小型本地矩阵求解器和 WebGL 纹理着色器完成；这样避免数 MB 的通用计算机视觉依赖。若 WebGL 不可用，则保留裁剪与旋转，并明确关闭上下、左右透视。
- 四角坐标足以计算照片中的旋转、对边长度差、平行线收敛和透视严重程度，因此“拍摄是否够正”可以本地判断；同一组坐标不包含深度、侧面轮廓或受控光照信息，因此不能证明卡片本体是否平整。
